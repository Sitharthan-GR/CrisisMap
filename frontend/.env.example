# API base URL (use Vite proxy in dev — leave as /api/v1)
VITE_API_URL=/api/v1

# Narrated demo video on the help page (defaults to /crisismap-demo.mp4 in public/)
# VITE_DEMO_VIDEO_URL=/crisismap-demo.mp4
